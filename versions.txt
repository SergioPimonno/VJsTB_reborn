# Список версий LED Scheme Designer, доступных для обновления из приложения
# (Настройки → «Обновить версию…»). Формат построчно:
#   версия|тег релиза|available(yes/no)|заметка
# Строки с # и пустые строки игнорируются. available=no — версия скрыта из списка
# (черновик/снятая с публикации), но строку можно оставить для истории.
#
# ВАЖНО: начиная с этой версии 2.0 актуальный клиент этот файл БОЛЬШЕ НЕ ЧИТАЕТ —
# список версий теперь отдаёт сервер (синглтон VERSION_MANIFEST, редактируется из
# ledscheme-admin, см. update.VersionManifest в клиенте). Файл оставлен ТОЛЬКО ради
# обратной совместимости — все версии до этой (1.3–2.0, выпущенные до переноса на
# сервер) всё ещё читают его напрямую с raw.githubusercontent.com при попытке
# обновиться, и без него получают 404. Не удалять, пока такие клиенты остаются в
# ходу; при выпуске новой версии обновлять ОБА места — и этот файл (для старых
# клиентов), и VERSION_MANIFEST на сервере через админ-консоль (для новых).
1.3|v1.3|yes|
1.4|v1.4|yes|
1.5|v1.5|yes|
1.6|v1.6|yes|экспорт NovaLCT для контроллера + LCTPresetMaster (несколько экранов, Complex Screen)
2.0|v2.0|yes|облачные проекты, интерактивные разъёмы и автозаполнение блок-схемы, переработанные настройки, редактор масок, интерактивные примеры
2.1|v2.1|yes|наземный конструктив с 3D-редактором, экспорт под After Effects, обновлённая спецификация проекта
2.2|v2.2|yes|калькулятор транспорта (кофры/машины, автоматическая раскладка загрузки), уточнённый расчёт точек подвеса по нагрузке (отступ от края фермы, симметричная нагрузка на крайние точки), доработанный расчёт наземного конструктива (вынос базы под балласт, регулируемый коэффициент отгруза), экспорт NovaLCT без лишнего Complex-режима для экранов со скрытыми кабинетами, персонализация темы/шрифта, регулируемое качество (DPI) экспорта схем, расширенная отмена действий, синхронизация через ledschemedesigner.ru
2.3|v2.3|yes|сетевой менеджер (сети/устройства/связи, живой пинг, скан IP, таблица адресов, конфликты IP), горячая клавиша «Перемещение»/«Соединение» на общей схеме (Пробел), редактор общей схемы (стили пропуска провода, буфер узлов Ctrl+C/Ctrl+V, перетаскивание сегмента маршрута, именованная отмена с ПКМ-списком), кнопка «Экспорт схемы…» (текущий вид в JPEG) на этапах Питание/Сигнал, резерв на уровне отдельной карты контроллера, раздельная ориентация гнёзд разъёмов для сигнала и питания, «Предпочтения» перегруппированы по этапу работы + матричный вид, «Форма экрана» отдельным окном, папка экспорта по умолчанию, блок «Статистика сцены», доработки калькулятора транспорта, фиксы экспорта NovaLCT (контроллер не первый в сцене больше не даёт пустой шаблон; экран с нескольких контроллеров пишется пере-индексированными кусками с выбором отдельный screen / сохранить положение; контроллер с Complex- и обычными экранами больше не блокируется — всё собирается в один мультиэкранный .scr со смешанными Standard/Complex-блоками); автоблок «Легенда портов» на общей схеме сигнала (таблица Экран/Main/Backup, перетаскиваемая и масштабируемая, с отдельным экспортом в PNG), ручная привязка блока-контроллера к контроллеру сцены («Связать с…»), перетаскивание экранов и контроллеров в списках для смены порядка (с пересчётом уже расключённых портов)
2.4exp|v2.4exp|no|ЭКСПЕРИМЕНТАЛЬНЫЙ релиз (доступность включается отдельно) — архив проектов, масштаб интерфейса, закрепление/открепление инспектора прерига переживает переключение проекта и перезапуск, параметры экрана по умолчанию для новой сцены, раздельный учёт крепежа стыков ферм (пальцы/шпильки/бобышки), уточнённый расчёт нагрузки точек подвеса по реальной длине фермы
2.4|v2.4|yes|переработка гнёзд и связей общей схемы (гнёзда по периметру блока, ориентация потока, роль интерфейса определяет сторону и цвет линии, транзитные разъёмы, автоматическая ортогональная трассировка под 90° с перетрассировкой, пучки связей в одно гнездо, печатный пресет оформления, блоки сетевого оборудования, легенда линий, переключатель «Современный/Классический» способ отрисовки), многовыделение связей общей схемы (Shift/Ctrl+клик), настраиваемый отступ трассировки от блоков (тот же отступ — и от гнезда, и от посторонних блоков, и от своего блока), переработанное окно визуализации прерига, кнопка сборки спецификации сразу по всей сцене, доработки наземного конструктива и расчёта нагрузки точек подвеса из 2.4exp (теперь окончательные), исправление потери данных при обрыве процесса во время сохранения (атомарная запись workspace.json/library.json) и дополнительная резервная копия на сервере, исправление нумерации портов в легенде для контроллеров с несколькими картами Ethernet+Fiber
2.5|v2.5|no|ПРЕДРЕЛИЗ — группы экранов в дереве Сетапа (объединение выделенных экранов шифтом, цвет и сворачивание группы), библиотека контроллеров слита в пресеты оборудования (с одноразовой миграцией с предпросмотром), параметры экспорта этапа «Вывод» (формат схем JPG/PNG/WebP/PDF, DPI, качество) и окно прогресса экспорта, корнер-превью сцены на этапах Питание/Сигнал, видимость типа кабинета в палитре, флаг «Прописывать как стандартный кабинет» при экспорте в NovaLCT и размер кабинета Standard по портам, автоперенос сетей из общей схемы в сетевой менеджер (коммутаторы приходят как сетевое оборудование), линии питания/сигнала кабинетов в спецификации, настраиваемая скорость прокрутки списка портов на этапе «Сигнал», экспорт под After Effects переделан по образцу pixl Grid (прекомпозиция на каждый экран, guide-слои пустот и разметки координат), вход в аккаунт больше не сохраняется между запусками — вместо этого можно запомнить логин и пароль
