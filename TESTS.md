# Карта тестов — VJsTB_reborn (клиент)

Инвентаризация всего тестового покрытия клиента для быстрой ориентации агентов.
Собрано из `src/test/java/**` на момент коммита `690d1e8` (v2.2).

- **Фреймворк**: JUnit 5 (`junit-jupiter` 5.10.3), `maven-surefire-plugin`.
  Компиляция под Java 21 (`maven.compiler.release=21`).
- **Прогон**: `mvn test` в корне `VJsTB_reborn`. Перед релизом — полный прогон,
  0 failures (скиллы `run-tests`, `release-client` шаг 5).
- **Всего**: 43 тест-класса, ≈350 тест-методов (`@Test`).
- **Принципы проекта**:
  - Никакой реальной сети — sync-клиенты тестируются на зафиксированном JSON,
    ping/scan тестируют только построение команды, не запуск процесса.
  - Бинарные форматы (NovaLCT `.scr`) — golden-байтовые регрессии против
    РЕАЛЬНЫХ файлов NovaLCT, подтверждённых загрузкой обратно.
  - Визуальные схемы (rigging / vehicle load image writers) — только
    структурные инварианты размера, не побайтовое сравнение.
  - GUI — «дымовые» тесты: строят реальные окна и закрывают, не показывая;
    пропускаются в headless-среде.

---

## Корневой пакет `com.vjstb.ledscheme` — 13 классов, ≈170 методов

### `AppModelTest` (≈118) — доменное ядро без UI
Сценарий проект→сцена→экран→цепочки, пересчёт характеристик, undo,
round-trip JSON. Крупнейший класс проекта. Покрывает: создание иерархии и
статистику; цепочки питания/сигнала (undo, перегруз против кастомного рейтинга
разъёма, backup-порты на уровне порта и контроллера, split-link,
многоэкранные цепочки); сокеты Schema-узлов (авто-добавление при разводке,
backfill, отсутствие безграничного накопления, выживание ручного сокета при
ресинке); CRUD библиотек (кабинеты, кабели, network devices, cable length
profiles) + export/import round-trip по имени/режиму; скрытие кабинетов
(маскирование формы, исключение из статистики и цепочек); авто-раскладка
экранов без пересечений + bottom-aligned auto-arrange; per-cell override типа;
поворот кабинета; контроллеры как общие для сцены, card-based подсчёт портов,
bandwidth/pixels взаимозависимость; Schema-узлы/рёбра со scope на сцену+режим,
undo удаления узла с рёбрами, групповое move/delete как один шаг; equipment
presets (CRUD, применение, независимые port id); `librarySync*` — большой блок
(добавление/обновление по server id, промоушен личного элемента, union из
shared+personal, миграция FK ссылок экрана при промоушене
cabinet/hoist/structure-frame/controller, case-insensitive name match,
удаление unreferenced shared, сохранение shared-удаления при живой ссылке,
custom equipment category, игнор неизвестного kind); `autoPopulateSchema*` —
блок (только разведённые экраны, только текущий вызов, не переподключает
вручную удалённое ребро, для питания заполняет существующие distro-разъёмы по
порядку без создания узлов, пропускает mismatched spare); `updateScreenStructure*`
/ `toggleStructureFrameCell*` — сид-грид, ручные удаления в новых границах,
per-cell override, row-2 усиление; маски (`setMaskColor` общий для всех
placements экрана, seed легаси per-placement на загрузке); проекторы (add по
сцене, delete по id, throw без выбранной сцены).

### `ChainInteractionControllerTest` (15) — построение цепочки без Swing/мыши
Старт по выбору цели; добавление кликом/протяжкой и стрелками; завершение по Esc
с commit-колбэком; клик по тому же кабинету не дублирует; `startFor` во время
построения коммитит предыдущую; авто-старт по клику на неразведённый кабинет
через starter (и блокировка, если starter вернул null); ПКМ во время построения
удаляет кабинет из активной цепочки; скрытый кабинет не может начать/войти;
занятый кабинет пропускается (клик/протяжка/стрелки), но re-edit переигрывает
собственные кабинеты вопреки occupancy-проверке; курсор зажат в границах сетки;
re-edit опустошённой ПКМ цепочки не трогает исходную.

### `SchemeRendererShapeTest` (9) — треугольные кабинеты (Task #91/v1.5)
Прямой угол в одном из 4 углов ячейки по `CabinetType.rotationDeg` (0°=левый
нижний, далее по часовой); произвольный угол снапится к ближайшей четверти;
per-cabinet override побеждает override типа; anchor соединения для треугольника
— его центроид, для прямоугольника — центр ячейки; неквадратная ячейка
сохраняет соотношение сторон при любом повороте.

### `WireLabelDialogTest` (9) — обогащение номинала линии
Суффикс переходника для единственной подсказки; уже скомбинированные метки не
трогаются; неоднозначные/неизвестные подсказки не трогаются; locked-сокет
предлагает bare + adapter варианты, включает подходящий кабель из библиотеки и
подходящие length-profiles, фильтрует кабель для неверного разъёма, не
фильтрует при неизвестном разъёме.

### `SchemaLoadCalcTest` (6) — сквозной расчёт нагрузки схемы питания (Task #87)
Узел (щит/дистрибьютор) сравнивает уходящую нагрузку с ёмкостью входных
разъёмов, включая рекурсию через промежуточные узлы. Перегруз против дефолтного
derating (одна фаза); полная ёмкость без derating; фазность и нулевой derating
поднимают ёмкость; экран на двух прокси делит нагрузку по числу линий, а не по
полной сумме; заниженный wire count на одном ребре не раздувает долю другого;
рекурсивная сумма через промежуточный узел. `resetCalcDefaults` перед каждым
тестом (изоляция от тестов, трогающих `Defaults.apply`).

### `SchemeExportTest` (4) — экспорт схемы в JPEG
Рендер изображения ожидаемого размера; запись `.jpeg` в выбранную папку; DPI в
JFIF-метаданных; `dpiScale` множит пиксельные размеры.

### `SignalStagePanelTest` (2) — регрессия выбора порта
Выбор порта (хоткей/кнопка) + клик по кабинету реально начинает и сохраняет
цепочку сигнала; `selectPortByHotkey` ничего не делает без контроллера в сцене.
(До фикса `refresh()` безусловно обнулял только что выбранный порт.)

### `AfterEffectsJsxWriterTest` (2) — экспорт `.jsx` под After Effects
Образец канваса 3584×1280, три экрана. Композиция нужного размера, слой на
экран с позицией/anchor point по границам экрана; имя PNG совпадает с
`AfterEffectsJsxWriter#maskFilename`.

### `ResolumePresetExporterTest` (1) — по образцу реального файла Resolume
Канвас 3584×1280 с тремя экранами (Left_Portal/Center/Right_Portal) —
`ScreenSetup` XML соответствует раскладке канваса.

### `GuiSmokeTest` (1) — дымовой тест сборки UI
Строит главное окно с реальными данными (проект, сцена, экран, цепочка) и
закрывает, не показывая. Ловит ошибки связывания панелей. Пропускается headless.

### `ScenarioFeatureGuiTest` (1) — сквозной тест «Интерактивных примеров» в реальном UI
Синк с сервера (реальный JSON → `AppModel.applyLibrarySyncItems`) → список
сценариев → проигрыватель, включая реальный хит-тест хотспота (клик мимо не
продвигает, внутри — продвигает, шаг без хотспота — кнопкой «Далее»).

### `PowerStagePanelChainRowClickTest` (1) — реальная доставка Swing-события
Клик по строке цепочки через `SwingUtilities.getDeepestComponentAt` +
`dispatchEvent` (Swing доставляет самому глубокому компоненту — JLabel, не
всплывает к JPanel-родителю). Резюмирует редактирование той цепочки.

### `UpdateSystemTest` (1) — формирование ссылок на ассеты релиза
Без сети. Имена ассетов соответствуют конвенции публикуемого релиза. Разбор
манифеста версий переехал на сервер (см. `update/VersionManifestTest`).

---

## `com.vjstb.ledscheme.model` — 1 класс, 4 метода

### `ControllerTypeEthernetPoolTest` (4) — пул Ethernet-нумерации карт (баг H-серии Novastar)
Входная карта (только IN-порты) исключена из подсчёта Ethernet-пула;
`sendingCardAt` пропускает входную карту, а не просто сырой индекс; число портов
в пуле соответствует правильной карте; порты входной карты вообще не участвуют
в нумерации выходных.

---

## `com.vjstb.ledscheme.service` — 15 классов, ≈111 методов

### `StructureCalcTest` (22) — количество железа объёмной башни (Phase 2.1)
Формулы заданы пользователем напрямую (2 стакана на стык рам, болты = число рам
× 1.5, требуемый балласт ≈ вес экрана — см. `STRUCTURE_CALC_NOTES.md`). Счётчики
по user-ratio с base frame; удаление среднего сегмента рвёт стыки своего ряда;
балласт масштабируется ratio, контейнеры делятся по весу из библиотеки;
предупреждения о высоте (>5 м, башня выше экрана); авто-высота башни = физ.
высота экрана; число башен по фиксированному дефолтному шагу как fenceposts;
вертикальные рамы на башню из высоты рамы библиотеки; интервал перемычки = 1.5
высоты рамы; расширенные секции из выноса (вычесть обязательный модуль
первым); усиление в секциях расширения идёт в счёт вертикальных рам и стыков;
core base секции независимо переключаются; башни не генерятся там, где под ними
нет кабинетов (и не строятся, если пропал только нижний ряд); задний ряд
независим и короче переднего; перемычка/base-frame соединяют соседние башни
только когда обе валидны.

### `VehicleCalcTest` (15) — подбор минимально достаточной машины под кофры
(См. `VEHICLE_CALC_NOTES.md`.) `checkFit` без штабелирования суммирует полную
площадь; штабелирование уменьшает footprint; запас вокруг каждого footprint;
кофр выше кузова → fail с причиной; пересчёт штабеля на каждого кандидата;
toggle веса меняет исход. `recommend` выбирает наименьшую подходящую по
площади→высоте→грузоподъёмности, пусто если ничего не влезло.
`suggestCaseCount` округляет вверх; `sceneCabinetCount` исключает скрытые и
чужие сцены. Персистентность счётчиков кофров: per-scene, per-project, custom
(scene id + count), last-scope — все переживают reload.

### `RiggingCalcTest` (12) — распределение веса подвешенного экрана по точкам
(Метод грузовых площадей, см. `RIGGING_CALC_NOTES.md`.) Равномерный экран — ровно
по точкам; скрытые кабинеты не дают веса и не смещают распределение; over-capacity
флаг при превышении WLL тали; без capacity — никогда не флагуется; `HoistType`
из библиотеки важнее ручной capacity; висячий `hoistTypeId` откатывается на
ручную; `suggestPointCount` — геом. минимум 2 без известной ширины модуля, по
физическому пролёту, растёт при нагрузке > capacity; `compute` вставляет отступ
от края (500 мм), узкий экран пропускает отступ, tie-break к центру, не влево.

### `CableSpecCalcTest` (9) — округление длин и минимальный набор кусков
Округление вверх к ближайшей доступной длине; точное совпадение как есть; запас
толкает к следующей длине; null если ни один кусок не покрывает; `minimalKit` —
один кусок когда покрывает, сплайс когда нет, null при пустом каталоге;
breakdown группирует рёбра по округлённой длине, сообщает uncovered только при
пустом каталоге.

### `NovaLctCombineHelperTest` (9) — эфемерная сеточная раскладка экранов
`validateSlots()` / `combine()` на `ScreenSlot`. Вырожденный случай (контроллер
трогает ОДИН экран) совпадает с прямым `write` — контроллер-центричный путь
`resolver→combine→writeStandardCombined` есть надмножество подтверждённого
одноэкранного `NovaLctScrWriter#write`. Отказ на mismatched размере кабинета и
пересекающихся экранах; приём экранов разной высоты бок о бок с заполнением
зазора blank-сентинелом по точным координатам; split-separate-grouped;
разные Sending Card на разные ячейки одной сетки.

### `NovaLctMultiScreenWriterTest` (6) — golden-байты `writeStandardMultiScreen`
Против РЕАЛЬНОГО вывода NovaLCT. `twoPlainScreens` (2 экрана разного размера с
ненулевым Coordinate X зазором), `threePlusScreens` (3 экрана — формула хвоста
`133+40×N` раньше экстраполировалась по 2 точкам), `multiCardAcrossScreens` (2
экрана на разных физ. Sending Card). Отказ на пустом списке; header сообщает
реальное число экранов и shared JSON содержит обе записи; один экран в списке
всё равно идёт через мультиэкранный путь.

### `NovaLctScrWriterTest` (4) — golden-байты одноэкранного писателя
Против байтов, сохранённых самой NovaLCT и подтверждённых загрузкой обратно.
`t1.scr` (1×1, 0 записей), `scr1.scr` (4×3, 11 записей, column-major обход,
происхождение поля `0x14b`). Первая карта/порт пропускают blank-сентинел при
выборе header-дефолта; скрытый кабинет пишет явную blank-запись, не опускается.

### `NovaLctScrParserTest` (3) — round-trip парсера
`NovaLctScrParser` не имеет реальных образцов .scr для >2 экранов или
нестандартного кабинета — только round-trip через свой подтверждённый писатель.
Два теста фиксируют ИЗВЕСТНЫЕ ограничения: `ANCHOR` — фикс. константа для
128×128 (для другого разрешения парсер молча не найдёт записей);
`fillMissingOrigin` не восстановит происхождение (0,0) при единственной записи
порта (1×1 экран).

### `NovaLctControllerResolverTest` (3) — резолвер кабинетов любого экрана сцены
Находит кабинеты ЛЮБОГО экрана сцены (не только текущего), раскладывает
scene-wide номер порта на (карта, порт-в-пуле) для конкретного контроллера,
минуя ограничение `ScreenLogic#cardAndLocalPort`. Простой одно-карточный;
H2-подобный дву-карточный; цепочка через два экрана одной сцены.

### `NovaLctComplexScrWriterTest` (2) — golden-байты Complex Screen
Байты `ether1.scr`/`ether2.scr` из реального экрана 4×6 (нижний ряд 128×64,
остальные 128×128), отличаются РОВНО одним Ethernet-портом (Sending Card 1,
Port 1 vs Port 2). Формат реверс-инжинирен декомпиляцией реального
`ScreenInfoToArray1600`.

### `NetworkScanServiceTest` (8) — чистые функции сканера
`expandRange` — перечисляет все адреса включительно, один адрес при from==to,
пересекает границу октета, отвергает from>to / слишком широкий диапазон /
битый адрес. Windows и POSIX — один пакет с коротким таймаутом. Реальный ping
не тестируется.

### `NetworkIpConflictsTest` (5) — детект конфликтов IP
Нет конфликта при уникальном IP; конфликт при общем IP у двух устройств;
пустой IP никогда не конфликтует; одно устройство не конфликтует с собой;
сравнение trimmed и case-insensitive.

### `NetworkDeviceLabelsTest` (4) — приоритет источников имени устройства
Своя пометка > узел схемы > каталожный тип > заглушка. Общая логика для
`ui.NetworkCanvasPanel` и `ui.NetworkAddressTableDialog`.

### `NetworkPingServiceTest` (3) — построение команды ping
Windows через `cmd` с UTF-8 code page и continuous ping; Linux/mac — plain
continuous. Реальный запуск процесса намеренно не тестируется.

### `StructurePickMathTest` (6) — луч выбора в 3D-виде конструктива
Центральный пиксельный луч от глаза к центру; верхне-левый пиксель — влево-вверх;
луч попадает в бокс прямо перед ним, промахивается мимо; выбирает ближний из
двух перекрывающихся; бокс позади начала луча не попадает.

---

## `com.vjstb.ledscheme.store` — 1 класс, 3 метода

### `LibraryStoreTest` (3) — разделение библиотеки и воркспейса на два файла
Одноразовая миграция старых `workspace.json` (сохранённых ДО разделения, где
библиотечные списки лежали в корне рядом с проектами); свежая установка без
файлов → пустая библиотека; `AppModel` держит библиотеку вне `workspace.json`.

---

## `com.vjstb.ledscheme.sync` — 3 класса, 10 методов

### `AuthClientTest` (4) — разбор ответов login/register без сети
Успешный login; извлечение `message` из тела ошибки; откат на поле `error`;
откат на générический текст для битого тела. Зеркалит `AuthController`.

### `ProposalClientTest` (4) — разбор ответа `POST /api/proposals` без сети
Отправленный proposal; pending-список; пустой pending-список; `message` из
ошибки.

### `LibrarySyncClientTest` (2) — разбор `GET /api/library/changes` без сети
Пустые changes; элементы со всеми полями. Зеркалит `LibraryController`.

---

## `com.vjstb.ledscheme.ui` — 9 классов, ≈47 методов

### `VehicleLoadCanvasPanelTest` (15) — автораскладка «Разместить всё»
(См. `VehicleLoadCanvasPanel#autoPlaceAll`, `VEHICLE_CALC_NOTES.md`.) Заполняет
всё при достаточном месте; штабель перед упаковкой; сообщает остаток при
нехватке площади / при слишком высоком; поворот когда влезает только боком;
заменяет существующие размещения, а не добавляет; размещения в границах кузова;
пустой кузов → всё в остаток. `addPlacement` штабелирует на выбранный стек того
же типа, откат на origin без совпадающего выбора / при полном стеке.
Вертикальные: кофр на торце без штабеля, откат на лёжа если слишком высок,
вертикально влезает больше на ограниченной площади. `clearAll`.

### `OutputPathsTest` (6) — папка вывода по умолчанию
`resolveRoot` тестируется напрямую (не через `defaultFolder`, та делает
`mkdirs` в реальном home). Откат на builtin home-video путь без override / при
пустом override / при null settings; используется настроенный export root;
`defaultFolder` вкладывает проект и сцену под настроенный корень, опускает
уровень сцены при null сцене.

### `SchemeRendererPortLabelTest` (5) — метка порта на расключении
Баг: нумерация порта не совпадала с выбранным в контроллере. Первый
Ethernet-порт первой выходной карты — «1», не сырое «3» (после двух fiber);
для второй выходной карты — с префиксом карты; одно-пуловый контроллер — просто
номер; резолвит контроллер, хранящийся на ДРУГОМ экране той же сцены;
multi-controller сцена — префикс контроллера по индексу в scene-wide списке.

### `VersionManifestTest` (5) — сравнение версий `isNewer`
(Этот класс в пакете `update`.) Простое major.minor; недостающие сегменты = 0;
НЕ лексикографически; пустой/null current → кандидат новее; пустой/null
кандидат → никогда не новее.

### `RecentColorsChooserPanelTest` (4) — общий список недавних цветов
`remember` ведёт ОДИН static-список на весь процесс. Новейший первым; повтор
цвета двигает в начало, не дублирует; кап размера; игнор null.

### `SnapMathTest` (4) — снап значения к цели по силе
Полная сила — точно в цель; нулевая — сырое значение; половина — на полпути;
клампит силу вне диапазона.

### `MathFieldsTest` (3) — вычисление выражения в поле формы
Регрессия: «Высота, px» в «Новый канвас» не вычисляла `208*8` при потере
фокуса. `commitEdit()` вычисляет выражение на integer-спиннере, всё ещё
принимает простое число, принимает уже сгруппированное число.

### `UiKitTest` (3) — форматирование мощности `UiKit#fmtPower`
Ватты через plain `fmt` + суффикс; киловатты масштабируются и округляются до 2
знаков; ноль в обеих единицах.

### `RiggingSchemaImageWriterTest` (3) — смоук `RiggingSchemaImageWriter`
Непустая картинка размером под экран + точки; шире с бóльшим числом точек;
обрабатывает скрытые кабинеты и тип нулевой ширины. Только инварианты размера.

### `VehicleLoadSchemaImageWriterTest` (4) — смоук `VehicleLoadSchemaImageWriter`
Непустая картинка; больше при бóльшем `dpiScale`; не падает на
повёрнутых/штабелированных/с примечанием размещениях; пустые размещения всё
равно рисуют контур кузова.
