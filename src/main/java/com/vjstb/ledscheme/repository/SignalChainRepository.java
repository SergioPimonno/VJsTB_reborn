package com.vjstb.ledscheme.repository;

import com.vjstb.ledscheme.domain.SignalChain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SignalChainRepository extends JpaRepository<SignalChain, Long> {
}
