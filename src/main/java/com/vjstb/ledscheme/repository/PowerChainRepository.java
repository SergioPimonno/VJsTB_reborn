package com.vjstb.ledscheme.repository;

import com.vjstb.ledscheme.domain.PowerChain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PowerChainRepository extends JpaRepository<PowerChain, Long> {
}
